datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Podcast {
  id          Int    @id @default(autoincrement())
  podcastNAME String
  episode     String
  videoID     String
  categories  String
}
